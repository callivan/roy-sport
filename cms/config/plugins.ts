module.exports = ({ env }) => ({
  upload: {
    config: {
      provider: "local",
      breakpoints: {},
    },
  },

  placeholder: {
    enabled: true,
    config: {
      size: 10,
    },
  },
});
